

type SpaceArgs = {
  config: {
    fidgetConfigs: {
      [key: string]: any
    }
    layoutID: string;
    layoutConfig: {
      [key: string]: any
    }
  };
  isEditable: boolean;
}

const Space = ({ config, isEditable }: SpaceArgs) => {
  return (
    <>
      
    </>
  );
};

export default Space;