

type SpaceArgs = {
  config: {
    fidgetConfigs: {
      [key: string]: any
    }
    layoutID: string;
    layoutConfig: {
      [key: string]: any
    }
    layoutID: string;
  };
  isEditable: boolean;
}

const Space = ({ config, isEditable }: SpaceArgs) => {
  return (
    <>
      
    </>
  );
};

export default Space;