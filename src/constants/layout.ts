/**
 * Common layout constants used across the application
 */

/** Padding used for mobile views (in pixels) */
export const MOBILE_PADDING = 12;

/** Height of the tab bar for padding the content (in pixels) */
export const TAB_HEIGHT = 72;