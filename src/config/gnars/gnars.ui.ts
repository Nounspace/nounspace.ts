import type { UIConfig } from "../systemConfig";

export const gnarsUI: UIConfig = {
  primaryColor: "#ff8a3d",
  primaryHoverColor: "#ff7130",
  primaryActiveColor: "#f25f1f",
  castButton: {
    backgroundColor: "#ffe564",
    hoverColor: "#ffd142",
    activeColor: "#ffbe2c",
  },
};
