# Fast build environment variables
SKIP_ENV_VALIDATION=1
NEXT_PRIVATE_SKIP_SIZE_LIMIT=1
ANALYZE=false

# Disable heavy features during fast builds
NEXT_TELEMETRY_DISABLED=1
DISABLE_ESLINT_PLUGIN=true
